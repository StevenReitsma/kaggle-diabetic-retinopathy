###Contains scripts which can be used as benchmarks.

Probably mostly used for profiling data operations over large datasets, such as
preprocessing, reading and augmenting using various thread models.
